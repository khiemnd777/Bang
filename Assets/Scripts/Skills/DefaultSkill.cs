using UnityEngine;

public class DefaultSkill : Skill
{
    public override void Use()
    {
        base.Use();
        // var playerFieldSlots = BattleFieldManager.instance.fieldSlots; 
        // var opponentFieldSlots = BattleFieldManager.instance.opponentFieldSlots;

    }
}